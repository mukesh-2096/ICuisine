import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFFA5211);
  static const Color primaryLight = Color(0xFFFF8E5E);
  static const Color background = Color(0xFFFA5211);
  static const Color white = Colors.white;
  
  // Shadow colors
  static Color primaryShadow = primary.withOpacity(0.3);
}
